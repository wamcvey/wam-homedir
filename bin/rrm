#!/bin/sh

rm_path() {
	if [ -f "$1" ]
	then
		rm "$1" && echo rm "$1"
	elif [ -d "$1" ]
	then
		rmdir "$1"  2>/dev/null && echo rmdir "$1"
	fi
}

for path
do
	while rm_path "$path" 
	do

		path=`dirname "$path"`
	done
done
